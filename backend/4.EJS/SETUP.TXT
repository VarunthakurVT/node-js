1) EJS INSTALL 
  --- pip i EJS
2) config ejs 
-----app.set("view engine ",'ejs')
3] views folder banao 
4) usme ejs file banao
5) send ki jagh render karo 


*)static files are used to add images frontend js setup karna 
steps to setup
1)create a folder public 
2)inside it make 3 folders like images javascript and the styleshteet
3)config the express static 
by like in the express app.use(express.static('/public'))
4)understand the path in ejs like when we link the ejs to the style.css 
do like the link rel="styleshteet" href="/style/cssfile name'>
